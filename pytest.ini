[pytest]
# 缓存目录固定放在 .tmp/ 下：仓库根目录只保留源码、规则与文档。
cache_dir = .tmp/.pytest_cache
testpaths = tests
addopts = -ra --strict-markers --strict-config
markers =
    contract: 核心协议契约测试
    integration: 真实 CLI 与文件系统边界的集成测试
    security: 对抗测试（注入、越权、缓存失效、失败关闭）
