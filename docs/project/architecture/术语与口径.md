# 术语与口径（唯一口径表）

> 本文是 `docs/project/architecture/` 的**口径唯一真相源**：图（两份 `.drawio`）、文（三份 `.md`）与代码数据
> 三处说法不一致时，先看本文 §1–§5，再回到 `src/`、`policies/`、`knowledge/`、`registry/`、
> `validation/`、`adapters/`、`api/`、`tools/`、`.github/workflows/phase-8.yml` 复核。
>
> 与 [README.md](README.md) 的约定一致：**只描述当前代码实际做到的事**，数字必须实测并写清命令。
>
> 配套：[功能清单.md](功能清单.md) · [使用说明.md](使用说明.md) · [规则文档转化为规则.md](规则文档转化为规则.md) ·
> 图：[技术架构.drawio](技术架构.drawio)（2 页）· [技术流程.drawio](技术流程.drawio)

## §0 一页速览

每行一条口径：先给结论（≤45 字），再给图、文、代码三列锚点；带数字的口径见 `§5` 的实测命令。

| # | 口径（结论） | 图 | 文 | 代码与数据（事实来源） |
| --- | --- | --- | --- | --- |
| 1 | 架构分**六层**，序号与层名固定 | 技术架构 第 1 页 `b1`–`b6` | 功能清单 §3 各子系统小节 | 见 §1 映射表 |
| 2 | **台阶 1–7** 指"文档 → 规则"的转化链路七步 | 技术架构 第 2 页容器标题就是「台阶 1」–「台阶 7」（`st1`–`st7`） | 规则文档转化为规则 §3 | `knowledge/corpus.yaml`、`policies/` |
| 3 | **Phase 0–8** 指实施阶段，与台阶无关 | `p2a9` 提及 Phase 4 / 7 / 8 | 功能清单 §1.1、使用说明 §2–§9 | `docs/project/learning/phase-0/` … `phase-8/` |
| 4 | 图内 **①②③** 是三道通过条件（门禁），不覆盖七个台阶 | `p2d1` / `p2d2` / `p2d3` | 规则文档转化为规则 §3、§4 | `retrieval.cli verify`、`retrieval.indexer`、`policy.loader` |
| 5 | 判定只有一条路径 | `c3_3` | 功能清单 §1 "三条唯一" | `policy.engine.evaluate` |
| 6 | 判定结果只有三值 `allow` / `allow_with_warnings` / `block` | `c3_3`、`p2a8` | 使用说明 §0 退出码表 | `Decision`，`src/policy/models.py:255` |
| 7 | 证据只产证据、不判 allow / block | `c4_2`、`b6` | 功能清单 §3.1 `evidence.py` 行 | `src/policy/evidence.py` |
| 8 | 授权绑定参数、单次使用 | `c4_3` | 使用说明 §5 | `action_hash`，`src/enforcement/action.py` |
| 9 | 台账与摘要链都是追加写 | `c6_1` | 功能清单 §8 "审计链"行 | `src/enforcement/ledger.py`、`src/enforcement/audit.py` |
| 10 | **skipped ≠ 通过**（跳过要写明原因） | `p2x4` | 使用说明 §12 FAQ | `skipped_rules`，`src/policy/checkers.py:33` |
| 11 | 未知一律拒绝、失败关闭 | 第 2 页副标题 `t2` | 功能清单 §1 定位 | 逐层失败语义，见 §3 |
| 12 | 能力上限由声明推出，不由运行时猜 | `c2_2` | 功能清单 §3.5 | `adapters/<agent_id>/manifest.yaml` + `adapters/approved.json` |
| 13 | 退出码 `0` / `1` / `2` 是 CLI 的结论口径 | `b6_cap`：0 通过 / 1 违规或漂移 / 2 配置错误 | 使用说明 §0、§1 | `src/policy/check.py:71`–`73` |
| 14 | 编排终态只有 `blocked` / `needs_human` / `failed` | 图上无 | 使用说明 §9 | `STATUS_BY_CODE`，`src/orchestration/errors.py:127` |
| 15 | 决策协议带版本，消费方看不懂必须拒绝 | `c3_4` | 功能清单 §3.1 `models.py` 行 | `SCHEMA_VERSION = "1.0"`、`POLICY_VERSION = "phase-1"` |
| 16 | 三层规范模型决定"能不能阻断" | `p2note` | 规则文档转化为规则 §2 | `docs/mirrors/<mirror>/**` → `corpus.yaml` → `policies/**` |
| 17 | 数字口径一律实测（本文 §5 附命令） | `c6_3`、`c6_4` | 功能清单 §5、§6；使用说明 §10 | 见 §5 |
| 18 | 文风口径：概括 + 精确锚点两层 | 图节点标签两层写法 | 三份 `.md` | 见 §7 |
| 19 | 流程页的 6 个分组是**技术抽象**，不是六层架构 | 技术流程 `s1`–`s6`（已改 A–F） | 技术流程的图注 | 见 §1 末段 |

## §1 分层名与包含关系

六层是图的骨架：**层 → 子系统 → 模块**。层名与 `技术架构.drawio` 第 1 页**完全同名**，
下表第 3 列就是图要重建的父子关系，并与功能清单 §3 的子系统小节一一对上。

| 层（规范名，含序号） | 图 1 容器 | 该层包含的子系统（功能清单小节） | 代码与数据位置 |
| --- | --- | --- | --- |
| ① 消费方 / 入口 | `b1` | §3.7 编排层（消费者）· §3.8 dsh 接入（真实 Runtime）· 合成协议消费者 · 人与 CI | `src/orchestration/`、`src/adapters/dsh/`、`tools/api_loop.py`、`.github/workflows/` |
| ② 接入与协议转换 | `b2` | §3.5 多 Agent 适配 · §3.6 的 HTTP 传输边界 · §3.8 的 Hook 线协议 | `src/adapters/`、`src/policy_api/app.py`、`src/adapters/dsh/hooks.py` |
| ③ 判定核心（唯一判定路径） | `b3` | §3.1 判定核心 | `src/policy/` |
| ④ 能力层 | `b4` | §3.2 规范检索 · §3.3 受控执行 · §3.4 代码验证器 · §3.6 服务化 | `src/retrieval/`、`src/enforcement/`、`src/validators/` + `validation/`、`src/policy_api/` |
| ⑤ 数据与契约 | `b5` | 功能清单 §4 数据与契约清单 | `policies/`、`knowledge/`、`registry/`、`validation/`、`adapters/`、`api/`、`tests/fixtures/` |
| ⑥ 证据与门禁 | `b6` | 功能清单 §5 仓库脚本 · §6 测试与证据 | `.tmp/` 运行期产物、`tools/phase_evidence.py`、`tools/check_*.py`、`tools/*_loop.py`、CI workflow |

**跨层子系统**（一条子系统只挂一个层会失真，图上要显式标「跨层」）：

- §3.6 Policy API：② 传输边界 + ④ 服务化能力（`policy_api/app.py` 只做协议转换，判定仍走 `policy.engine.evaluate`）；
- §3.8 dsh 接入：① 真实 Runtime + ② Hook 转换（`src/adapters/dsh/adapter.py` 转事件，`hooks.py` 决定 exit 0 / 2）。

**包含关系现状**（实测 `python .tmp/arch_lint.py`，2026-09-21 15:59）：

| 页 | 可见节点 | 容器 | 有归属 | 仍平铺 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 技术架构 第 1 页 | 44 | **6**（`b1`–`b6`，均含 `container=1`） | 35 | 3 | 层 → 能力节点的父子关系**成立**；根级只剩 2 个标题 + Legend |
| 技术架构 第 2 页 | 31 | **11**（`st1`–`st8`、`g1`–`g3`） | 22 | 2 | 初检（15:59）为 19 / 0 / 0 / 19 的**零包含**，现已重建为「台阶 / 门禁 → 产物」 |
| 技术流程 | 28 | **6**（`s1`–`s6`，均含 `container=1`） | 19 | 3 | 泳道 `A`–`F` → 技术节点成立；根级是 2 个标题 + 说明 |

第 1 页与流程页「仍平铺」的根级节点是 2 个标题 + 6 个层（泳道）标题 + 1 个 Legend / 说明：
层标题就是容器自身标题（`swimlane;startSize=36`），不是额外节点；第 2 页必须新建容器，标题与说明留在根级。

**层名的规范名是短名**：写成「① 消费方 / 入口」这种“序号 + 短名”。图上容器标题可以带括号锚点
（`b1`–`b6` 已加，例如 `① 消费方 / 入口（src/adapters/dsh/）`），但正文与标题引用层名时一律用短名，锚点按上表第 4 列给（§6.5 第 8 条）。

**两套"六个分组"不要混**：第 1 页的六层是**架构分层**（消费方 / 接入 / 判定 / 能力 / 数据 / 证据）；
`技术流程.drawio` 的 6 个泳道是**技术抽象分组**（输入 / 解析 / 存储 / 判定 / 接口 / 工程）。
后者已把序号从 ①–⑥ 改成 `A`–`F`，避免与六层共用一套序号。口径冻结为：
**层 = 第 1 页六层；流程页只写 A–F，不写"第 N 层"。**

## §2 编号对照表

三套编号**正交**：台阶是链路步骤，Phase 是交付批次，①②③ 是三道通过条件。

| 台阶 | 名称 | 图 2 节点 | 图内门禁 | 主要 Phase | 对应文件 / 目录 |
| --- | --- | --- | --- | --- | --- |
| 台阶 1 | 镜像与哈希登记 | `st1`（`p2a1`） | 不挂门禁（产物被 ① 校验） | 无 Phase（镜像流水线） | `docs/mirrors/<mirror>/**` + `manifest.json`、`tools/mirror_docs.py` |
| 台阶 2 | 语料登记 | `st2`（`p2a2`） | ①（`p2d1` ⊂ `st2`） | Phase 3 消费 | `knowledge/corpus.yaml`、`src/retrieval/corpus.py` |
| 台阶 3 | 分块与索引 | `st3`（`p2a3`） | ②（`p2d2` ⊂ `st3`） | Phase 3 | `src/retrieval/chunker.py`、`indexer.py`、`store.py` |
| 台阶 4 | 提炼（唯一人工步骤） | `st4`（`p2a4`、`p2note`） | 无 | 无 Phase | 人工；`src/policy/models.py` 的 `SourceRef` |
| 台阶 5 | 规则文件 | `st5`（`p2a5`） | ③（`p2d3` ⊂ `st5`） | Phase 0 / 1 | `policies/<domain>/<ID>.yaml`、`src/policy/loader.py` |
| 台阶 6 | 验证器覆盖 | `st6`（`p2a6v`） | 无 | Phase 5 | `validation/validators.yaml`、`src/validators/` |
| 台阶 7 | 生效、溯源与变更管理 | `st7`（`p2a6`）**内含** `st8` | 无 | Phase 1 + 传播 3 / 7 / 8 | `RuleSet.identity`、`retrieval.cli rules` |
| 判定与消费（**不是台阶 8**） | 台阶 7 内嵌的子容器：规则生效后的判定与消费方 | `st8`（`p2a7`–`p2a9`、`p2x4`，⊂ `st7`） | 无 | Phase 1 / 4 / 7 / 8 | `policy.engine.evaluate`、`src/enforcement/`、`src/policy_api/`、`src/orchestration/` |

三点必须写清，否则"台阶 ↔ 门禁"会被读成并列关系：

1. **门禁 ① 挂在台阶 2**：校验的是台阶 1 的产物（条目在不在 manifest 里、许可文件在不在、sha256 一致），
   所以 `g1` ⊂ `st2`，台阶 1 自己不挂门禁（`src/retrieval/corpus.py`）。
2. **门禁 ② 由台阶 3 执行、依赖台阶 7 的登记**：登记了 `rule_sources` 才强制。当前已登记 38 条规则 → 原文 chunk 的溯源
   （2026-09 起不再是空表），解析不到 chunk 会让索引 run 失败。
3. **门禁 ③ 只覆盖台阶 5**：台阶 6、7 与「判定与消费」没有图内门禁，图侧必须显式标注原因；台阶 6 原无节点，已补 `st6`。
   两组门禁概念见下（不许混算）：
**"两道加载门禁"与"三道通过条件"是两组概念**（规则文档转化为规则.md:21 与图 2 的表面冲突）：

| 说法 | 指什么 | 编号关系 |
| --- | --- | --- |
| 门禁 ①②③ | 转化链路上的三道**通过条件**（图 2 的 `p2d1` / `p2d2` / `p2d3`） | 图上编号，见上表 |
| 两道**加载**门禁 | **加载阶段**就报错的语义类别：规则加载器、验证器注册表 | 规则加载器**就是 ③**；验证器注册表门禁（`RegistryError`）落在判定侧失败关闭（`p2x4`），图上**无独立编号** |
| 合计口径 | 引用时写"**图上 3 道编号门禁 + 1 道无编号的注册表加载门禁**" | 不要数成"4 道门禁"，也不要漏掉第 4 处硬失败点；两套计数不许混 |

Phase 侧（`功能清单.md` §1.1 的主题列，落点目录可实测）：

| Phase | 主题与落点 | 与台阶的关系 |
| --- | --- | --- |
| Phase 0 / 1 | 最小规则系统、决策协议与范围匹配（`src/policy/`） | 台阶 5、台阶 7 的判定路径与协议世代 |
| Phase 2 / 6 | dsh Adapter 与 Hook、多 Agent 适配（`src/adapters/`） | 台阶 7 的消费方与多消费方 |
| Phase 3 | 离线规范检索（`src/retrieval/`） | 台阶 1（消费镜像产物）、台阶 2–3 |
| Phase 4 / 5 | 受控执行与代码验证器（`src/enforcement/`、`src/validators/`、`validation/`） | 台阶 6、台阶 7 的生效与台账 |
| Phase 7 / Phase 8 | Policy API 与 LangGraph 编排（`src/policy_api/`、`api/`、`src/orchestration/`） | 台阶 7 的对外接口与编排消费方 |

## §3 判定链路术语

| 术语 | 一句话定义 | 锚点 |
| --- | --- | --- |
| 判定（Decision） | 六层里唯一会给出结论的产物 | `src/policy/models.py:255` |
| 严重级别 → 判定 | 无违规 `allow`；`info` / `warning` `allow_with_warnings`；`error` / `critical` `block` | `src/policy/models.py:172`、`:972` |
| 证据（EvidenceBundle） | 验证器产出的确定性事实，只产证据、不判定 | `src/policy/evidence.py`、`src/policy/checkers.py:42` |
| 授权（action_hash） | 覆盖工具 schema、规范化参数、主体、权限与上下文摘要的短时效凭据 | `src/enforcement/action.py` |
| 台账（ledger） | 记录"这个 `action_id` 已执行过"的追加写 JSONL，拦重复执行 | `src/enforcement/ledger.py` |
| 摘要链（audit chain） | `sequence` + `prev_digest` 的追加写链；能发现中间改动，**删尾与整链重写发现不了** | `src/enforcement/audit.py` |
| 跳过（skipped_rules） | 规则没参与判定，必须写明维度原因；**跳过不是通过** | `src/policy/checkers.py:33`、`src/policy/engine.py` |
| 失败关闭 | 缺证据、工具不可用、版本未知一律取更严结论，绝不默认放行 | 逐层检查，如 `checkers.py` 的 `uncovered_checker_violation` |
| 能力上限 | Adapter 的 `manifest` 只声明上限；写类动作仍需证据 + allow + pre-check | `adapters/<agent_id>/manifest.yaml`、`AGENTS.md` 第 29 条 |
| 证据不足与不一致 | 事后验证缺证据记 `repair_required` / `inconsistent`，回滚能力缺失写 `unsupported` | `src/enforcement/postcheck.py` |

四条容易混的口径：

- **`skipped_rules` ≠ 通过**：Phase 2 的 Hook 只给上下文，证据类 checker 的规则进 `skipped_rules`；
  Phase 6 的写类动作没有 evidence provider 时直接 `evidence_unavailable` 阻断。
- **「跑不了」 ≠ pass**：`tools/dsh_sandbox_loop.py` 环境不具备时按环境跳过（退出码 0 + reason + 复现命令）。
- **判定结果与 CLI 措辞分开**：状态名只写三个枚举；`policy.check` 输出的 `PASS` 字样是措辞，不是口径名。

## §4 退出码与终态

三个层次不要混：判定结果是业务结论，退出码是 CLI 结论，编排终态是工作流结论。
锚点是 `src/policy/check.py:71`–`73` 与 `src/orchestration/errors.py:127`。

| 退出码 | 含义 | 锚点 |
| --- | --- | --- |
| `0` | `allow`；或命令成功 | `src/policy/check.py:71` |
| `1` | `block` / `allow_with_warnings`（含需人工审批的 block）；或漂移 / 无结果 | `src/policy/check.py:72`、`exit_code_for` |
| `2` | 配置或执行错误：规则损坏、上下文不完整、未知 checker、请求非法、审计链损坏 | `src/policy/check.py:73` |

注意：`allow_with_warnings` 与 `block` 都退 `1`，脚本不能靠退出码区分二者，要用 `--json` 读载荷。

**`tools/ci_local.py` 只有 `0` / `1`**：它把子步骤的非零码收敛成自己的 `1`（`tools/ci_local.py:253`–`:285`），
不是 `0 / 1 / 2` 口径的所有者。引用这三值时锚 `src/policy/check.py:71`–`73`，不要锚 `ci_local.py`。

| 终态（RunStatus） | 什么时候出现 | 例子（`FailureCode`） |
| --- | --- | --- |
| `blocked` | 平台侧说不清，禁止继续 | `POLICY_UNAVAILABLE`、`EVIDENCE_UNAVAILABLE`、`TRACE_FORGED` |
| `needs_human` | 上限击穿或审批要人来决定 | `APPROVAL_MISSING`、`LIMIT_REPAIR_ROUNDS`、`SIDE_EFFECT_UNKNOWN` |
| `failed` | 编排自身损坏 | `STATE_VERSION_UNKNOWN`、`CHECKPOINT_CORRUPT`、`ENGINE_UNAVAILABLE` |

未登记的失败码一律按 `failed` 处理（`src/orchestration/errors.py:161` 的 `status_for`），不猜、不放行。

**实测退出码样例**（canon 2026-09-21 16:40，命令 + 结果）：

| 命令 | 退出码 | 为什么是这个码 |
| --- | --- | --- |
| `python -m policy.check --check-rules` | `0` | 43 条规则全部加载成功 |
| `python -m retrieval.cli rules --rule SEC-008` | `0` | 已登记的溯源：`SEC-008@1 <- 02_输入验证、注入与文件处理/SQL_Injection_Prevention_Cheat_Sheet.md#…` |
| `python -m retrieval.cli rules --rule ARCH-001` | `1` | `没有登记的来源溯源` —— 项目自订规则（`source.kind: project-policy`）本就没有镜像来源，"无结果"也是 `1` |
| `python -m retrieval.cli vector \"code review\"` | `1` | `status: unavailable (index_missing)` —— 检索不可用是显式状态 |

**退出 0 ≠ allow**：`python -m adapters.cli inspect` 实测退出 `0` 但 `outcome_code=context_error`（失败关闭发生在载荷里）。
读退出码只能判断"命令有没有跑到最后"，结论要看决策载荷的 `decision` 或 `outcome_code`。

## §5 数字口径

每个数字都附实测命令；命令在仓库根目录、Windows PowerShell 下执行，
`python` 指 `.venv` 解释器，导入 `src/` 的子命令先设 `$env:PYTHONPATH = "src"`。

| 项 | 实测值 | 实测命令 | 核对 |
| --- | --- | --- | --- |
| CI 具名步骤 | **44** | `(Select-String -Path .github/workflows/phase-8.yml -Pattern '^      - name:').Count` | canon |
| `tools/` 三种计数 | 顶层 `.py` **26** / 顶层文件 **27** / `git ls-files tools` **35** | `(Get-ChildItem tools/*.py).Count`、`(Get-ChildItem tools -File).Count`、`(git ls-files tools).Count` | canon；**三个口径不同**，引用时写清用的是哪条 |
| 闭环脚本 `*_loop.py` | **6** | `Get-ChildItem tools/*_loop.py -Name` | canon |
| 规则条数 | **44**（39 条 `standard` + 5 条 `project-policy`） | `python -m policy.check --check-rules`（输出 `rules: 44`，exit 0） | Lead 2026-09 实测 |
| 语料数据集 / 条目 | **6 / 42** | `python -m retrieval.cli verify`（输出 `datasets=6 entries=42`，exit 0） | Lead 2026-09 实测 |
| 索引文档 / chunk / 溯源 | **42 / 1013 / 141**（溯源覆盖 **38** 条规则） | `python -c "import sqlite3;c=sqlite3.connect('.tmp/retrieval/index.sqlite3');print(*[c.execute('select count(*) from '+t).fetchone()[0] for t in ('documents','chunks','rule_sources')])"` | Lead 2026-09 实测 |
| 分块规模 | 章节 **720**（多片 **160**）、超预算 `oversized` **8**、截断 `truncated` **0** | `python -c "import sqlite3;c=sqlite3.connect('.tmp/retrieval/index.sqlite3');print('sections',c.execute('select count(*) from (select 1 from chunks group by document_id,heading_anchor)').fetchone()[0],'multipart',c.execute('select count(*) from (select 1 from chunks group by document_id,heading_anchor having count(*)>1)').fetchone()[0],'oversized',c.execute('select count(*) from chunks where oversized=1').fetchone()[0])"`；空章节 **18** 见 run 报告 `index_runs.empty_sections` | canon（2026-09-22 新增） |
| 验证器 / checker / scope 维度 | **7 / 6 / 6** | `python -m validators.cli registry`；`python -c "from policy.checkers import SUPPORTED_CHECKERS as S;print(len(S))"` | canon |
| 受控工具 | **9**（dsh 6 + orchestrator 3） | `python -m enforcement.cli registry --list`（输出 `9 个工具`） | canon |
| Adapter manifest / 合成消费者 | **3 / 3** | `Get-ChildItem adapters -Recurse -Filter manifest.yaml`；`Select-String -Path tools/api_loop.py -Pattern 'http-api'`（`http-api` 由该脚本构造，不在 `adapters/` 下） | canon |
| 离线镜像 / 检索术语 | **6 / 24** | `(Get-ChildItem docs -Recurse -Filter manifest.json).Count`（迁移后 `docs/` 只有 `mirrors/` 与 `project/` 两个子目录，**6 套第三方镜像都在 `docs/mirrors/` 下**，本项目自产文档在 `docs/project/` 下）；`python -c "import yaml,io;print(len(yaml.safe_load(io.open('knowledge/query_expansion.yaml',encoding='utf-8'))['terms']))"` | canon |
| 全量测试（**对外统一口径**） | **收集 1202 = 1200 passed / 1 failed / 1 skipped**（2026-09-22 22:00 取数） | `$env:PYTHONPATH='src'; python -m pytest -q` | 唯一跳过是"本机不允许创建符号链接"；**唯一失败不在规则语料改动范围内**：`tests/unit/test_precheck.py::test_check_list_is_complete_for_governed_actions` 由并行的受控执行改动（`src/enforcement/precheck.py`，21:42:36）引入。注意：与本次会话并行还有一条改 `tests/` 的工作流，**这个数字会随它移动**，引用时请连同取数时刻一起写 |
| 文本规范 / 凭据扫描门禁 | 文本 **502 文件 0 问题**；凭据扫描 **501 文件 0 处命中、退出 0** | `python tools/check_text_conventions.py`；`python tools/secret_scan.py` | Lead 2026-09 复测（新增规则语料与夹具后文件数 374 → 502）；旧值见 §6.3 第 15 条 |
| 阶段证据脚本 | **退出 1（当前为红，根因是同一个既有失败）** | `python tools/phase_evidence.py` | canon：`.tmp/artifacts/phase-8-evidence.json` 里 `suites.tests/integration` = `exit_code 1 / failures 1 / result fail` |
| 图侧一致性 / 文风 | 全部 **0 处** | `python tools/check_arch_canon.py`；`python tools/check_arch_style.py` | Lead 2026-09-22 复跑仍 0 处（坏链那一项当时的临时脚本未升格为门禁） |
测试数字必须带实测次数。2026-09-22 22:00 取数：收集 1202（1200 passed / 1 failed / 1 skipped）。
那 1 个失败来自并行的受控执行改动（`src/enforcement/precheck.py`），不是规则语料；且并行工作流仍在加用例，引用这个数字必须连取数时刻一起写。
那条长期红的既有缺陷 `test_changed_from_git_uses_the_working_tree`（git 的 `core.quotepath` 把中文路径
C 引号转义）已在 `src/validators/cli.py` 里用 `-c core.quotepath=false` 修掉，不再计入。

Lead 2026-09-22 复跑两次：改动前 `1109 / 1 / 1`（123.85s），改动后一次 `1108 / 2 / 1`（106.57s）。
多出的那个是安全测试里的"临时目录按验证器隔离"用例
（`tests/security/test_validator_adversarial.py` 的 `test_temp_directories_are_isolated_per_validator`）：
它用 `sorted(.tmp/validators/*)[-1]` 取"最新"目录；当该目录下已有残留（本次 78 个）时，
字典序最大的未必是刚建的那个。清掉 `.tmp/validators/` 后，该文件实测 `12 passed`、退出 0——
是测试取目录的方式不稳，与语料、图、文档无关。

## §6 核对记录

本节记录谁在什么时候核对了什么，锚点一律写到 `文件:行` 或图的 cell id。

### 6.1 谁核对了什么（按时间）

| 时间 | 谁 | 核对了什么 | 方式与结论 |
| --- | --- | --- | --- |
| 15:40–15:50 | canon | 两份 `.drawio` 的 57 + 37 个 cell、三份 `.md` 的标题结构 | `.tmp/canon/extract_drawio.py` 逐 cell 导出（`id` / `parent` / `value` / 坐标） |
| 15:50–16:00 | canon | §5 全部数字、全量测试与失败用例定性 | 逐条命令实跑；`pytest -q` 后隔离复跑两个失败用例 |
| 15:5x–16:05 | Lead | 图侧包含关系基线更正（第一版检测写错）、文风口径与基线 | 已并入 §1、§7 |
| 16:00–16:10 | texts → canon | 层名 / 台阶↔Phase / 门禁 / 数字逐条建议（A–G 段） | canon 逐条裁定：采 B、纠 E（`tools/` 三种计数）、定 C/D |
| 16:05–16:20 | canon ↔ diagrams | 口径表 v1 + 8 个待确认点 → diagrams 回执与落地 | `st8` ⊂ `st7`、`b5` 补 `knowledge/`、`b<N>_cap` 补小节号 |
| 16:25 | texts → canon | R2 回执 + 反证（两次全量都是 1109 / 1；`secret_scan` 退 1；四条退出码） | canon 采纳（数字写实测次数）、复跑确认三条退出码 |
| 16:40–16:50 | canon / diagrams | 逐 cell 复核图与 PNG、重跑门禁、复核 `.bkp`；diagrams 报补丁完成 | `arch_lint` 0 处、文本规范 359/0、PNG 三张尺寸一致、`.bkp` 不存在 |
| 16:55–17:15 | texts → canon | 文侧定稿（三份 md：文风归零、台阶↔Phase 表、门禁措辞、测试数字两次实测） | canon 复核：`arch_style` 0 处、无坏链、`使用说明` 无 Phase 9–13、证据 JSON 根因核对通过 |
| 17:20 | canon / Lead | 凭据扫描告警定位：canon 引用串改写后自身归零；`app.js:401` 需把 `secret-scan: allow` 移到命中行 | `secret_scan.py`：命中数 2 → 1 |
| 16:55–17:05 | canon | **R3 定稿核对**：§6.3 全部条目逐条复检、跨文件数字审计（`.tmp/canon/number_audit.py`）、四道门禁重跑 | `arch_lint` **0 处**、文本规范 **359 / 0**、文风 **0 处**、markdown 链接 **0 处坏链** |
| 2026-09-22 17:40–18:00 | Lead | **台阶 3 补齐**：chunker 的打包分支、chunk_id / text_hash、FTS5 口径、三条索引不变式、run 台账与 index_version 逐条读源码核对 | 新增 `规则文档转化为规则.md` §3.1–§3.5；索引库实测 375 章节 / 535 chunk / 90 多片 / oversized 6 / truncated 0；另确认一处静默丢字（见 §6.3 第 17 条） |
| 2026-09-22 18:00–18:20 | auditor（只读子代理） | 14 张 PNG 逐张实看 + 逐 cell 导出图上的可核查断言，对代码与数据逐条实测 | 报告：重复 1 处（图 2 ↔ `08-操作总览`，已删）、不准确 5 条（2 条已改，2 条待 draw.io 环境）、其余 9 组断言（6 条规则 / 44 步 CI / 6 个闭环脚本 / 27 篇 535 chunk / 7 个验证器 / 四条判据等）实测准确 |
| 2026-09-22 18:00–18:20 | Lead | **整理 `tech-detail/` 文件夹**：逐图比对主题、重跑 `build.py` 校验产物可重建、核对四道门禁 | 删除重复图 `08-文档转规则-操作总览`（与图 2 同题）、原 09/10 顺次改为 08/09、删除 draw.io 备份与 PNG 脱钩的隐患；`build.py` 产物逐字节可重建（10/10 SAME）；四道门禁全 0 |

### 6.2 与任务书给定口径的偏差（canon 复核后的更正）

| 给定说法 | 实测 | 依据 |
| --- | --- | --- |
| 第 1 页 `b1`–`b6`、流程页 `s1`–`s6`"都没有 `container=1`" | **都有** `container=1;collapsible=0` | `技术架构.drawio:13,31,49,67,85,103`、`技术流程.drawio:13,25,37,49,61,73` |
| 第 1 页每层"5 个子节点" | 是 **4 个能力节点 + 1 条 `b<N>_cap` 说明** | `c1_1`–`c1_4` 与 `b1_cap` 均 `parent="b1"` |
| 第 2 页"33 个单元挂在根上" | 成立：33 个 cell = **19 个可见节点 + 14 条边** | `.tmp/arch_lint.py` 报"可见节点 19，容器 0" |
| 使用说明 "Phase 0–13" | 是**章节号 0–13**（14 节），不是 Phase 编号；全文没有 Phase 9–13 | `使用说明.md` 标题结构，Phase 只出现在 §2–§9 与 §11 手册表 |
| 全量测试 "1110 passed / 1 failed / 1 skipped" | canon 实测 **1108 passed / 2 failed / 1 skipped**（总数同为 1111） | §5 两行、§6.3 |

### 6.3 图 / 文 / 代码不一致清单（实检，写清位置）

核对方式：两份 `.drawio` 逐 cell 导出（`.tmp/canon/extract_drawio.py`），三份 `.md` 逐条 grep 取 `文件:行`，
代码与数据实跑命令（§5）。下表是 **2026-09-21 16:40 复核**后的状态，不是初检时的状态。

| # | 位置 | 现象 | 复核状态（16:40） |
| --- | --- | --- | --- |
| 1–4 | `技术架构.drawio`：`c6_4` / `legendt2` / 第 2 页 19 个平铺节点 / 台阶 6 无节点 | 五个 `*_loop.py`、cell id 泄漏、零包含、缺 `validation/validators.yaml` | **全部已修复**：6 个 + 命令、去 cell id、11 个容器（`st1`–`st8`、`g1`–`g3`）、新增 `st6`+`p2a6v`（逐 cell 复核） |
| 5–7、12 | `功能清单.md:4`–`5` 坏链 / `:196`「35 个文件」 / §3 缺层名与标注 | 五个英文名坏链、曾疑计数过期、缺「④ 能力层」「⑥ 证据与门禁」 | **坏链与层名已修复**（中文实名 + §3 层归属映射表）；**「35 个文件」撤回**（`git ls-files tools` = 35，原文对，错的是我的命令） |
| 8 | `使用说明.md:340`、`:345`、`:369` | 用例数曾写「1110 passed + 1 skipped」 | **已修复**：现为「收集 1111 = 1109 passed + 1 failed + 1 skipped」，并写明两次实测与耗时 |
| 9–11 | `.技术流程.drawio.bkp` / 三份 `.md` 无 535 / `b6` 锚点漏 `.github/workflows/` | 未跟踪备份、数字缺失、锚点不全 | **全部已修复**：`.bkp` 已删（检查数 360→359）、535 已出现在图与文、`b6` 锚点补齐 |
| 13 | `技术架构.drawio` `b<N>_cap` | 只有能力描述，没有功能清单小节号 | **已修复**：六条 cap 已补 `（功能清单 3.7 / 3.8）` 等（canon 复核 value） |
| 14 | `规则文档转化为规则.md:9`、`:19`、`:20` | 「两道加载门禁」与图内 ①②③ 表面冲突 | **已修复**：现写「两道加载门禁：规则加载器（就是图内门禁 ③）与验证器注册表」，与 §2 小表一致 |
| 15 | `docs/project/engineering-policy-platform/designs/console/assets/app.js:401` | `tools/secret_scan.py` 曾**退出 1**（命中 bearer 令牌正则；本文不贴具体串） | **已修复（2026-09-22 复测退出 0）**：命中的那行已改写成不含令牌形态的文本；`:401` 现为 `GET http://127.0.0.1:8088/v1/health/live …` |
| 17 | `规则文档转化为规则.md` 台阶 3（2026-09-21 版共 14 行） | 只写"去 front matter、按标题分块、超长再切"，分块规则 / 身份 / 检索口径 / run 台账全缺；`truncated` 的记账纪律也没写 | **已补齐（2026-09-22）**：§3.1–§3.5 五节；并如实登记一处静默丢字——`docs/mirrors/dora-capabilities/core/continuous-integration.md:45` 的 1253 字行被切成 1200 字且 `truncated=0`，**53 字无记录** |
| 18 | `tech-detail/08-文档转规则-操作总览` | 与 `技术架构.drawio` 第 2 页同题（台阶 1–7 + 门禁 ①②③），同一件事画了两张图 | **已删除（2026-09-22）**：原 `09` / `10` 顺次改为 `08` / `09`；它独有的"每步命令与产物"由第 2 页与 `规则文档转化为规则.md` §7 覆盖（说明留在 `tech-detail/README.md`） |
| 19 | `tech-detail/03-检索-SQLite-FTS5` 的"分块"节点 | 图上写"chunker：标题层级 + **CJK 逐字切分**"，把检索口径挂到了分块步骤上；分块产出的是**逐字原文**，CJK 切分只发生在 FTS 索引与查询侧 | **已改（2026-09-22）**：分块节点改为"标题层级 + 预算打包（原文不改写）"，CJK 口径移到 FTS5 索引库节点；规格与 PNG 同步重建 |
| 20 | `tech-detail/00-技术总览` 的 `.drawio` 与其 `.png` | `.drawio` 被 draw.io 打开后另存（16:46），PNG 是 16:39 由 `build.py` 渲染的——**两者已脱钩**，重跑 `build.py` 会覆盖手改布局 | **已收敛（2026-09-22）**：以 `build.py` 为唯一规格源重建两者；并新增 `.gitignore` 规则忽略 draw.io 自动备份（`*.drawio.bkp`、`.$*.drawio*`） |
| 21 | `技术架构.drawio:107`（`b6_cap`）与 `技术流程.drawio:85`（`g4`） | 图上把 `0 / 1 / 2` 退出码口径锚到 `tools/ci_local.py`；实测该脚本只有 `return 0` / `return 1`（`:253`–`:285`），并把子步骤的 `2` 收敛成 `1` | **已定位，待能用 draw.io 的环境落笔**：本机 draw.io Electron 实测起不来（`mojo platform_channel` 拒绝访问），只改 `.drawio` 会让 PNG 脱钩；替换文本见 §6.4 第 8 条 |
| 22 | `技术流程.drawio:40`（`n3_1`）与 `技术架构.drawio:243`（`p2a3`） | 两个"SQLite FTS5"节点都没列 `chunks_fts`——它正是 FTS5 虚表本身（`src/retrieval/store.py:105`） | 同上：与第 21 条同一次 draw.io 落笔一起改 |

### 6.4 未决项（谁主张什么、依据、为什么未决）

| # | 未决项 | 主张与依据 | 为什么未决 |
| --- | --- | --- | --- |
| 1 | 台阶 6 要不要补节点、台阶 4/6 的门禁标注 | canon 主张补节点并写明「无门禁」原因；diagrams 已补 `st6`+`p2a6v`，并标注台阶 4「没有图内门禁」、台阶 6「加载即报错、判定侧见 `p2x4`」 | **已收敛**（canon 16:40 逐 cell 复核） |
| 2 | 8 个子系统怎么在图上表达 | canon 原主张"按 §1 表挂到六层下"；diagrams 的实现是**层容器锚点 + `b<N>_cap` 小节号 + 跨层标记**，不新增 8 个节点 | **已收敛（canon 让步）**：映射权威是 §1 表 + 功能清单 §3 映射表，图以 cap 行做入口即可；逐 cell 复核六条 cap 已带小节号 |
| 3 | 全量测试的最终数字 | texts 独跑 2 次均为 `1109 / 1 / 1`（121.53s / 117.62s）；canon 含并发 1 次为 `1108 / 2 / 1` | **已落地（文侧与图都已按此写）**：三份 `.md` 与图都写"收集 1111、1 failed / 1109 passed / 1 skipped + 实测次数"（§6.5 第 13、16 条） |
| 4 | `.tmp/arch_lint.py` / `.tmp/arch_style.py` 是否升格为门禁 | canon 主张升格进 `tools/`：`.tmp/` 会被 `tools/cleanup.py` 删掉，README 的三处同改自检需要可复现门禁 | **已收敛（2026-09-22）**：已升格为 `tools/check_arch_canon.py` 与 `tools/check_arch_style.py`，登记在 `tools/README.md`，两者现均退出 0 |
| 5 | `.tmp/arch_lint.py` 的 `NUMBERS` 里写的是 `"1110"` | canon 主张改成 `"1111"` 与 `"1109"` | **已收敛（2026-09-22）**：`tools/check_arch_canon.py` 的 `NUMBERS` = `44 / 535 / 1109 / 1111`，`1110` 只作历史值写在工具注释里 |
| 6 | 层名是否含括号锚点 | canon 裁定：规范名 = 序号 + 短名；括号锚点保留在容器标题里但不构成规范名 | **已裁决**（§6.5 第 8 条） |
| 7 | 凭据扫描门禁曾为红（1 处） | canon 17:20 实跑：命中 `docs/project/engineering-policy-platform/designs/console/assets/app.js:401`；Lead 在 `:400` 写了 `secret-scan: allow`，但扫描器只认**命中行本身** | **已收敛（2026-09-22）**：该行已改写成不含令牌形态的文本，`secret_scan` 实测 `已扫描 373 个文件 / 没有发现疑似凭据`、退出 0 |
| 8 | 两张手工维护的图上有 4 处文字待改（退出码锚点 2 处、缺 `chunks_fts` 2 处） | §6.3 第 21–22 条实测确认；结论不缺，缺的是能导出 PNG 的 draw.io | **未决（缺工具，不是缺结论）**：本机 draw.io 桌面版无法导出 PNG，只改 `.drawio` 会让 PNG 与源脱钩。替换文本：① `b6_cap` 的 `（tools/ci_local.py）` → `（口径：src/policy/check.py:71–73）`；② `g4` 的 `0 通过 / 1 漂移 / 2 配置错误` → `0 通过 / 1 违规或漂移（退出码见 policy.check:71–73）`；③ `n3_1` 的 `documents / chunks / rule_sources` → `documents / chunks / chunks_fts / rule_sources`；④ `p2a3` 的 `SQLite FTS5：documents / chunks` → `SQLite FTS5：documents / chunks / chunks_fts`。改完必须重导三张 PNG |

### 6.5 裁决结论（canon）

1. **三套编号正交，禁止互相替代**：转化链路只写"台阶 N"，实施批次只写"Phase N"，图上条件只写"①②③"。
2. **层名冻结**为图 1 的六个全名（含 ①②③④⑤⑥ 序号），功能清单 §3 的小节名**从属**于层名，不与之并列。
3. **判定结果只写** `allow` / `allow_with_warnings` / `block`；`PASS` 只作 CLI 输出措辞，不作状态名。
4. **证据类概念固定四个词**：证据（`EvidenceBundle`）· 授权（`action_hash`）· 台账（`ledger`）· 摘要链（`audit chain`）。
5. **弃用词**："stage / 步骤 / 环节"不得指台阶（"步骤"只允许指 CI 步骤）；"阶段"单独出现时须写明是 Phase 还是台阶。
6. 第 1 页与流程页的**父子关系不需重建**，只补子系统层；第 2 页必须新建容器。
7. **8 个子系统按 §1 表挂到六层下**，跨层子系统（§3.6 挂 ②+④、§3.8 挂 ①+②）一处标"跨层"，不复制两份。
8. **层名的规范名不含括号锚点**：规范名 = 序号 + 短名；锚点可以留在图的容器标题里，但正文引用层名一律用短名，锚点按 §1 表第 4 列给。
9. **门禁有两组概念，不许混算**：「门禁 ①②③」是图 2 上的三道通过条件；「两道**加载**门禁」是加载阶段报错的语义类别，其中规则加载器就是 ③，验证器注册表门禁落在 `p2x4`。参见 §2 的小表。
10. **`使用说明.md` 的节号 0–13 保留**，但节号 ≠ Phase 号（可加 `（Phase N）` 标签，全文不得出现「Phase 9」–「Phase 13」）；流程页的 6 个分组写成 `A`–`F`，第 1 页的六层才叫「层」。
11. **`st8`（判定与消费）内嵌在 `st7`（台阶 7）里**，不是第八个台阶、也不与 `st7` 平级；口径表 §2 与图必须同形。
12. **门禁只挂在它实际执行的那一步**：`g1` ⊂ `st2`（① 校验台阶 1 的产物）、`g2` ⊂ `st3`（②）、`g3` ⊂ `st5`（③），台阶 1、4、6、7 不挂门禁并在图上写明原因。
13. **测试数字对外统一写**「收集 1202 = 1200 passed / 1 failed / 1 skipped（2026-09-22 22:00 取数；跳过项：本机不允许创建符号链接；失败项为并行工作流的 `test_precheck`，与规则语料无关）」。
14. **层 ⑤ 的括号锚点必须含 `knowledge/`**（diagrams 已改，canon 16:40 复核通过；原文漏了规则与语料的另一半载体）。
15. **门禁计数必须两套说清**：图上编号门禁 3 道（①②③），语义上的加载门禁 2 道（规则加载器 = ③、验证器注册表 = `p2x4`）。引用时写"图上 3 道编号门禁 + 1 道无编号的注册表加载门禁"，不许数成 4 道编号门禁，也不许漏掉第 4 处硬失败点。
16. **测试数字必须带实测次数**：写"收集 1202、1200 passed / 1 failed / 1 skipped"时必须同时写"取数时刻 + 独跑耗时"；把 1 次观测当事实违反本文的实测纪律。
17. **退出 0 ≠ allow**：`adapters.cli inspect` 实测退出 0 但 `outcome_code=context_error`；结论一律读决策载荷的 `decision` 或 `outcome_code`，退出码只说明命令跑到了最后。
18. **凭据扫描门禁已转绿（2026-09-22 复测）**：曾经命中 `app.js:401`，修法有二——把 `secret-scan: allow` 写在**命中行本身**，或把该行改成不含令牌形态的文本（仓库采用了后者）。见 §6.4 第 7 条。
19. **`tech-detail/` 的图只能由 `build.py` 生成**：产物（`.drawio` / `.png`）不得手改，手改必然让规格与图脱钩（§6.3 第 20 条就是这样发生的）；**同一主题只留一张图**，重复图删后者、并把被删图独有的信息并入保留方（§6.3 第 18 条）。
20. **图的锚点要挂在真正执行它的那一步**：`chunker` 产出逐字原文，CJK 切分发生在索引与查询侧——口径挂错步骤就是"描述不准确"（§6.3 第 19 条）。
21. **退出码 `0 / 1 / 2` 只锚 CLI**：口径的主人是 `src/policy/check.py:71`–`73`，
验证器、检索与受控执行三个 CLI 同口径；`tools/ci_local.py` 只退 `0` 或 `1`，它在失败信息里回显子步骤退出码，那不是它自己的结论口径。

## §7 文风口径（概括与精确兼顾）

用户要求：**概括性语言与精确描述性语言不能单一权重过大**。本节把它落成可检查规则，
三份 `.md`、两份 `.drawio` 与本文共用同一套阈值；**改文风先改本节**，再改文稿与图。

| # | 规则 | 阈值 |
| --- | --- | --- |
| 1 | 每节先一句概括句（是什么 / 能不能 / 结论），紧跟精确锚点 | 概括句 ≤ 45 字 |
| 2 | 术语首次出现给一句解释，之后直接用 | 括号内 ≤ 20 字 |
| 3 | 长句控制 | 单句 > 90 字的比例 ≤ 1/3 |
| 4 | 连续无标点 | ≤ 60 字 |
| 5 | 术语墙禁止 | 同一行反引号标识符 < 8 个 |
| 6 | 图节点标签两层：短标题（≤14 字）+ 精确标识（模块 / 枚举 / Phase） | 两层写法占比 ≥ 50% |
| 7 | 数字必须带实测命令 | 命令见 §5 |

**基线 → 目标 → 自检命令**：

| 项 | 基线（2026-09-21 15:59） | 现状（17:00） | 目标 | 自检 |
| --- | --- | --- | --- | --- |
| 文风问题总数 | 16 处 | **0 处** | 0 处 | `python tools/check_arch_style.py` |
| 长句占比 | 使用说明 37% / 功能清单 35% / 规则文档 39% | 全部 ≤ 33% | ≤ 33% | 同上 |
| 图节点两层写法 | 技术架构 5/63、技术流程 1/28 | 达标（≥ 50%） | ≥ 50% | 同上 |
| 文本规范 | 359 文件 / 0 问题 | 374 文件 / 0 问题 | 保持 0 | `python tools/check_text_conventions.py` |
| 图 / 文 / 口径表三处同口径 | 16 处不一致 | **0 处** | 0 处 | `python tools/check_arch_canon.py` |
| markdown 本地链接 | 5 处坏链 | **0 处** | 0 处 | 当时用 `.tmp/canon/linkcheck.py`（临时脚本，未升格为门禁；2026-09-22 未复测） |

**三处同改**：改图 / 改文 / 改口径必须同一次改完，否则图与文会再次脱钩。自检步骤见
[README.md](README.md) 的"维护方式"一节。
