# 一致性套件的探针工作区

Phase 6 的一致性套件用这个目录当**受控工作区**。它存在的理由：

- 场景要测"路径越界被拒绝"，就必须有一个真实的边界可比——因此这里是一个独立的、
  与仓库其他部分无关的小项目；
- 场景要测 layer 映射（`**/*_controller.py` → controller），因此文件名是有意义的；
- 它是**只读探针**：套件与闭环工具只在这里判定，不写入任何文件。

这些文件不是"示例代码"，它们的名字与层映射就是测试输入，改动前先确认没有场景依赖。
